export default function GroupItem(node: Element, { addClass, removeClass, }?: {
    addClass?: (className: string) => void;
    removeClass?: (className: string) => void;
}): {
    destroy(): void;
};
